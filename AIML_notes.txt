
macrof1 is good for analyzing how the model performs on classes overall 

accuracy would be skewed because it would see the model accurately predicting no_drought but doesn't represent the few d4, d3 etc cases there are that get missed most of the time 

class weight adds weights to the loss so that rare classes count more. it helps with imbalanced number of classes

discrete analog of CRPS is ranked probability score (RPS) i.e. crps for ordered categories 

multi-model ensemble loop can be brought in from that repo 
(i.e. start_iteration and end_iteration )

no need to do CRPS loss or the regression metrics i.e. me12 and mae12

we could use cross-entropy for training and Brier/RPS/calibration for evaluating the ensemble 

PIT/spread-skill diagrams could also be abandoned 

honestly. don't do it. focus XAI since dr huang is interested in that 

... but if you do, you could do 10 models 
ensembles are usually better than single models so the macro-F1 would get better 

the models are fast anyway 

